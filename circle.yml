machine:
  ruby:
    version: 2.3.0
deployment:
  staging:
    branch: /feature|chore|enhancement/
    commands:
      - mkdir -p build/forestland-for-sale
      - bundle exec middleman s3_sync --verbose
